<% errors = model.errors %>

<% if errors.any? %>
  <div class="alert alert-danger">
    <h2>Ошибок <%= errors.count %>:</h2>

    <ul>
      <% errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </div>
<% end %>
